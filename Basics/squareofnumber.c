
//                    DONT RUN CODE LIKE THIS YOU HAVE TO  COMMENT OUT 2 CODES FRIST THAN RUN 

#include<stdio.h>
int main (){

    int x = 5;         // in the code we have to  change the values of X again and again to  set new square 
    printf ("%d", x*x);
    return 0;
}

// taking input from user like we see in google 

#include<stdio.h>
int main (){

    int x;  
    scanf ("%d", &x);       // & = In computer language called ampercent 
    printf ("%d", x*x);
    return 0;
}

// in this code we taking input from user and than giving output

#include<stdio.h>
int main (){

    int x;  
    printf("Enter a Number =");
    scanf("%d", &x);       // & = In computer language called ampercent 
    printf ("Square of Number =  %d", x*x);
    return 0;
}
// above code work same  we just make the output look good 