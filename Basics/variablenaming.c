#include<stdio.h>
int main(){
  float radius = 25;
  
  float area = 3.14159265*radius*radius;  
  // we can use Letters also the answer will  be same but over code looks we use name the only reson.
          
  printf("area of cricle ⭕⚪ = %f", area); // we can use the "the statment also it shows in output "
 
 return 0;  
}

// simple just we are changing the name of box 'a' to 'area' & 'r' to 'radius' .